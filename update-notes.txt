ARCHITECTURE
Api Project
- log service {libsd3}
- db {libsd3}
- middleware
  -.OK http req/res logger (custom logrus-based middleware, fiber v3 native)
  -. reverse proxy
  -.OK healthcheck (/live + /ready as route-level handlers)
  -.OK 404 handler
  -.OK recover 
     :: https://github.com/gofiber/fiber/tree/v3/middleware/recover
  -.OK request-id (UUIDv4 X-Request-ID correlation header)
  -.OK timeout (30s context deadline per request)
  -. auth (jwt) // optional
- docs
  -. automatic swagger gen
  -. swagger aggregation
- api
  -. health check
  -. pingpong
  -. docs (untuk aggregator)
- worker pool (bounded background goroutine pool)
- docker (multi-stage alpine image, non-root user)

Desember 02, 2025
[+] Initial commit and file structure!

Desember 08, 2025
[+] Add swagger and basic endpoint (for aggregation uses)
[=] Refactor middleware with depedency injection style code

Desember 09, 2025
[+] Add user CRUD endpoints (create, get all, update password, delete)
[+] Add PostgreSQL repository layer with pgx/scany
[+] Add layered architecture: handler → service → repository
[+] Add bcrypt password hashing in UserService
[+] Add GlobalErrorHandler with consistent ResponseError envelope
[=] Refactor server into internal/server package

Maret 27, 2026
[+] Write comprehensive README.md for developer onboarding
[+] Add appsettings.ini with example configuration
[+] Document all API endpoints, project structure, and middleware

Maret 29, 2026  [Boilerplate 2.0]
[+] Add TimeoutMiddleware — 30s context deadline on every request
[+] Add /ready readiness probe that pings PostgreSQL (2s timeout)
[+] Add bounded background worker pool (internal/worker.Pool)
     :: defaultWorkerCount=4, defaultWorkerCapacity=128
[+] Add multi-stage Dockerfile (golang:1.24-alpine → alpine:3.21, non-root user)
[+] Add .dockerignore
[=] Update middleware registration order: RequestID → Logger → Recover →
     Timeout → HealthCheck → Favicon → RateLimiter
[=] Refactor HandlersRegistry to accept worker.Pool for background tasks
[=] Update GlobalErrorHandler: add default case using net/http.StatusText
[=] Update ResponseError envelope: BadRequestError and GatewayTimeoutError
     now use typed ResponseError struct
[=] Replace custom recover middleware with Fiber built-in + stack trace logging
[x] Remove dead code: UpdateUserPassword empty-userName guard (unreachable path)
[+] Add developer handbook (documentation/developer-handbook.md) with audit results,
     architecture diagram, step-by-step feature guide, and production checklist
[+] Add tests: requestid_test.go, timeout_test.go, recover_test.go, pool_test.go

Maret 30, 2026  [Fiber v3 Migration]
[=] Upgrade Go 1.24.6 → 1.25.8 (toolchain directive in go.mod)
[=] Upgrade Go Fiber v2.52.10 → v3.1.0
     :: import path: github.com/gofiber/fiber/v2 → github.com/gofiber/fiber/v3
[x] Remove github.com/gofiber/swagger (v2-only) → replaced by swaggo/http-swagger/v2
     via Fiber v3's built-in adaptor middleware
[x] Remove github.com/mikhail-bigun/fiberlogrus (v2-only) → replaced by a minimal
     custom logrus HTTP logger middleware (internal/middleware/logger.go)
[=] Migrate handler signature: func(c *fiber.Ctx) error → func(c fiber.Ctx) error
     (Ctx is now an interface in v3; no pointer receiver needed)
[=] Migrate context API: c.UserContext() → c.Context(),
     c.SetUserContext(ctx) → c.SetContext(ctx)
[=] Migrate request binding: c.BodyParser(&req) → c.Bind().Body(&req)
[=] Migrate requestid middleware: remove string ContextKey; access via
     requestid.FromContext(c) instead of c.Locals("requestid")
[=] Migrate healthcheck middleware: unified config (Liveness+Readiness) removed;
     register individual route handlers GET /live and GET /ready in the router
[=] Migrate app.Static("/", ...) → middleware/static registered as GET /* catch-all
[=] Migrate DisableStartupMessage from fiber.Config → ListenConfig parameter
[=] Update RateLimiter: add Next func to skip /live and /ready health probes
[=] Update all tests: app.Test(req, 5000) → app.Test(req, fiber.TestConfig{Timeout: 5s})
[=] Update middleware registration order: RequestID → Logger → Recover → Timeout →
     Favicon → RateLimiter (HealthCheck moved to route level)
[+] Add Immutable: true to fiber.Config — prevents zero-allocation data corruption
     when context values (c.Params, c.Query, c.Get) are captured by worker pool tasks

April 20, 2026  [Boilerplate 3.0 — Architecture Refactor]
Remove global database.Db variable. Implement full Dependency Injection:
     :: InitDatabases returns *database.Registry; DB is stored in request context
     :: Repository methods retrieve DB via database.DBFromContext(ctx)
     :: database.ErrNoDB returned when context is missing a DB connection
Complete Swagger feature flag: swagger UI routes now only registered
     when IsDevelopment = true; previously only swag init was gated
Multi-DC / Multi-Tenant support via database.Registry + MultiTenantMiddleware:
     :: Middleware reads X-Kunci header → looks up DB in registry → stores in context
     :: Comma-separated Kunci in appsettings.ini connects to multiple data centers
     :: First listed kunci is the default fallback when no header is present
     :: Middleware order updated: RequestID → Logger → Recover → MultiTenant →
        Timeout → Favicon → RateLimiter
Add cross-platform build scripts: build/build.sh dan build/build.bat
     :: Reads AppName from appsettings.ini for output binary name
     :: Supports GOOS/GOARCH overrides for cross-compilation
     :: Outputs to bin/<AppName>[.exe]
Simplify architecture — remove service-level UserServiceIface:
     :: HandlersRegistry.UserService is now *services.UserService (concrete type)
     :: Testability preserved at the repository layer (mock UserRepository interface)
     :: Handler tests refactored: mock at repo level → inject into real UserService
Timezone configuration via appsettings.ini (Timezone = Asia/Jakarta):
     :: time.Local overridden at startup so time.Now() returns localized time
     :: Invalid timezone names log a warning and fall back to UTC
Fix 404 handler: add JSON fallback when static/public/404.html is
     unavailable, preventing GlobalErrorHandler recursion on missing file
Add developer init scripts: setup.sh dan setup.bat
     :: Verifies Go version, downloads modules, installs swag, generates docs,
        confirms clean build, and prints onboarding instructions

April 20, 2026  [Boilerplate 3.0 — Follow-up Enhancements]
Extend Multi-DC kunci resolution to three sources (priority order):
     :: 1. Query parameter  ?KodeDC=<key>       (highest priority)
     :: 2. Request header   X-kode-dc: <key>
     :: 3. Nginx header     X-Forwarded-Prefix  (first path segment = key)
     :: Add multidc_test.go covering nil adapter, empty adapter,
        query-param priority, and X-Forwarded-Prefix extraction edge cases
Add TZ environment variable override for production deployments:
     :: Priority: TZ env var > appsettings.ini Timezone > UTC
     :: Allows Docker/K8s to inject timezone without touching config file
Update setup.sh and setup.bat with goswitch support:
     :: Reads required version from go.mod; installs exact version via
        golang.org/dl/go<version> if system Go differs
     :: Does NOT modify the developer's system Go installation
     :: Sets GO_CMD variable for remainder of script; prints usage tip
     :: Added step 6: run go test ./... after build
Update README.md: add Multi-DC routing table, Timezone priority table,
     goswitch section, middleware order table, updated config reference
Rewrite developer-handbook.md (Boilerplate 3.0):
     :: New sections: Multi-DC/Multi-Tenant Routing, Timezone Configuration,
        Developer Setup & goswitch
     :: Updated architecture diagram shows MultiTenant middleware and dual-DC
     :: Updated Adding a New Domain Endpoint example uses DBFromContext(ctx)
        instead of deprecated global database.Db
     :: Updated production checklist includes Swagger flag, TZ, Multi-DC items

April 20, 2026  [Boilerplate 3.1 — Lead Architect Audit]
CRITICAL: Fix error message leakage in GlobalErrorHandler:
     :: Non-Fiber errors previously passed raw err.Error() to clients —
        could expose SQL queries, internal paths, or stack traces
     :: GlobalErrorHandler now returns generic "An unexpected error occurred."
     :: Helper functions (InternalServerError, BadRequestError, GatewayTimeoutError)
        changed from handler-factory pattern func(error) fiber.Handler to direct
        func(fiber.Ctx, string) error — callers explicitly provide safe messages
     :: Removed per-status-code switch cases that duplicated the default branch
MAJOR: Add 15-second startup timeout to database initialization:
     :: initSingle now uses context.WithTimeout(context.Background(), 15s)
     :: Process fails fast if PostgreSQL is unreachable instead of hanging forever
MINOR: Document worker pool shutdown context behaviour:
     :: cancel() -> close(jobs) -> wg.Wait() order is intentional for cooperative shutdown
     :: Jobs needing to survive shutdown should create their own internal context
MAJOR: Add database connection close on graceful shutdown:
     :: New Registry.Close() method closes all registered *postgres.Database connections
     :: Called in main.go after pool.Stop() during graceful shutdown
     :: Prevents pgx pool leaks and lingering idle connections on PostgreSQL
MAJOR: Fix multidc_test to exercise actual middleware:
     :: TestResolveKunci_Priority was re-implementing resolveKunci logic inline
        instead of testing the real MultiDCMiddleware
     :: Rewrote test to use real middleware + middleware.KodeDCFromLocals(c)
MINOR: Add tenant key observability to MultiDCMiddleware:
     :: Resolved key stored in c.Locals via KodeDcLocalKey
     :: Enables downstream handlers and logging middleware to inspect which
        DC was selected for the request
MINOR: Simplify GlobalErrorHandler — remove handler-factory indirection:
     :: BadRequestError(err)(c) pattern created a fiber.Handler closure then
        immediately invoked it — added indirection with no benefit
     :: All cases inlined into the default branch of the switch
Update developer-handbook.md to Boilerplate 3.1

Mei 21, 2026  [Boilerplate 4.0 — Final Audit & Hardening]
[x] Fix: Reset() key mismatch di DcAdapter — dulu selalu no-op karena key
     menggunakan registryKey(kodeDc, appName) tapi cache menyimpan dengan bare kodeDc
[x] Fix: MultiDCMiddleware menggunakan context.Background() — sekarang
     menggunakan ctx.Context() agar timeout middleware benar-benar propagate
[x] Fix: EvictionInterval di DefaultAdapterConfig bernilai 0 — TTL eviction
     tidak akan berjalan proaktif; sekarang di-set 5 * time.Minute
[x] Fix: strconv.FormatInt(400, 32) menghasilkan "cg" bukan "400" dan
     response selalu HTTP 200 meskipun error — keduanya diperbaiki dengan
     menggunakan fibererror standard dan ctx.Status()
[x] Fix: /ready endpoint di-whitelist di rate limiter tapi tidak pernah
     didaftarkan sebagai route — sekarang registered dengan real HealthCheck()
[x] Fix: CloseAll() tidak dipanggil saat graceful shutdown — sekarang
     dbManager.CloseAll() dipanggil setelah pool.Stop()
[x] Fix: ServiceFactory.Build menerima log dan cfg sebagai parameter
     tapi struct sudah menyimpan keduanya — redundant params dihapus
[x] Fix: Logger middleware tidak menyertakan request_id di log fields
[x] Fix: Port bertipe string tanpa validasi — sekarang int dengan MustInt
[+] Add: .gitignore untuk .idea/, .vscode/, appsettings.ini
[=] Update: Developer handbook diperbarui dengan hasil audit lengkap
